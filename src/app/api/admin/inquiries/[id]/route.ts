import { db } from "@/db";import { inquiries } from "@/db/schema";import { eq } from "drizzle-orm";import { requireAuth } from "@/lib/admin-api";import { z } from "zod";
const update=z.object({status:z.enum(["new","contacted","in_progress","quoted","converted","closed","spam"]).optional(),priority:z.enum(["low","normal","high","urgent"]).optional(),assignedTo:z.string().nullable().optional(),internalNotes:z.string().nullable().optional(),archived:z.boolean().optional()});
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const u=await requireAuth();if(u)return u;const p=update.safeParse(await req.json());if(!p.success)return Response.json({error:p.error.issues[0]?.message},{status:400});const [row]=await db.update(inquiries).set({...p.data,updatedAt:new Date()}).where(eq(inquiries.id,Number((await params).id))).returning();return Response.json(row)}
export async function DELETE(_req:Request,{params}:{params:Promise<{id:string}>}){const u=await requireAuth();if(u)return u;await db.delete(inquiries).where(eq(inquiries.id,Number((await params).id)));return Response.json({ok:true})}
