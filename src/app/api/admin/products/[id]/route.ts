import { db } from "@/db";import { products } from "@/db/schema";import { eq } from "drizzle-orm";import { requireAuth,slugify } from "@/lib/admin-api";import { productInput } from "@/lib/admin-validation";
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){const u=await requireAuth();if(u)return u;try{const p=productInput.safeParse(await req.json());if(!p.success)return Response.json({error:p.error.issues[0]?.message},{status:400});const b=p.data,[row]=await db.update(products).set({...b,slug:slugify(b.slug||b.name),videoUrl:b.videoUrl||null}).where(eq(products.id,Number((await params).id))).returning();if(!row)return Response.json({error:"Product not found"},{status:404});return Response.json(row)}catch(e){console.error(e);return Response.json({error:"Unable to update product. Check that the slug is unique."},{status:500})}}
export async function DELETE(_req:Request,{params}:{params:Promise<{id:string}>}){const u=await requireAuth();if(u)return u;await db.delete(products).where(eq(products.id,Number((await params).id)));return Response.json({ok:true})}
